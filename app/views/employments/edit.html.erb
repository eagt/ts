<% provide(:title, 'Edit Employment') %>
<!-- When action is implemented fully, I must be able to update all the users details -->
<div class="employment edit">
  <h2>Edit Employment</h2>
  <%= link_to("<< Back to Menu", {:action => 'index'}, :class => 'back-link') %>
  <%= error_messages_for(@employment) %>
	  <%= form_for(@employment) do |f| %>
	  <%= render(:partial => 'form', :locals => {:f => f} ) %>
	<div class="form_buttons">
	   <%= submit_tag('Update Employment') %>
	</div>
  	  <% end %>
</div>