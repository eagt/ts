<% @page_tittle = "Delete Branch" %>



<div class="companies delete">
	<h2> Delete Branch </h2>
	<%= link_to("<< Back to Menu", {:action => 'index'}, :class => 'back-link') %>
		<br>
		<p></p>
			<%= form_for(@branch, :html => {:method => :delete}) do |f| %>
			<p class="reference-name"><%= "Are you sure you want to permanently delete #{@branch.name.upcase} branch?" %></p>
			<div class="form_buttons">
				<%= submit_tag("Delete Branch") %>
			</div>
			<% end %>
</div>