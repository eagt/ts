<% provide(:title, "Add New Contact Details") %>

   <h2><%= t(:create) + " " + t(:contact_detail) %></h2>

<!-- 
<%#= error_messages_for(@contact_detail) %>  -->

<!--<% @page_title = t(:contact_detail).pluralize(locale) %> -->

<%= link_to("<< Back to Menu", {:controller => 'contact_details', :action => 'index'}, :class => 'back-link') %>


<div class="contact_detail new">

		  <%= form_for(@contact_detail) do |f| %>

		    <%= render(:partial => "form", :locals => {:f => f}) %>


		    <div class="form_buttons">
		       <%= submit_tag("Create Contact Detail") %>


		   <!--  <div class="form-buttons"> 
		      <%#= submit_tag(t(:create) + " " + t(:contact_detail)) %> -->
		    </div>
		  <% end %>
</div>